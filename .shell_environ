# Environment utilities (e.g., set_envvar)
[ -f "$HOME/bin/environ_utils" ] || return
. "$HOME/bin/environ_utils"

# Linker library paths
#env_values=()
#set_envvar LD_LIBRARY_PATH "${env_values[@]}"

# Binary paths
env_values=(
"$HOME/bin"
)
set_envvar PATH "${env_values[@]}"

unset env_values
