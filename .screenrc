# Eduardo custom: default options in /etc/screenrc

# ------------------------------------------------------------------------------
# GENERAL SETTINGS
# ------------------------------------------------------------------------------
startup_message off
vbell off
vbell_msg ""
defscrollback 4096 
# enable mouse support
mousetrack on

# ------------------------------------------------------------------------------
# SCREEN KEYBINDINGS
# ------------------------------------------------------------------------------
# set escape key to '`' instead of 'Ctrl + a'
escape ``
# move input focus to the next region, use 'j' and 'k' as in vi.
bind j focus down
bind k focus up 

# ------------------------------------------------------------------------------
# TERMINAL SETTINGS
# ------------------------------------------------------------------------------
# use normal screen buffer, allows scrollback
termcapinfo xterm*|xterms|xs|rxvt* ti@:te@


# ------------------------------------------------------------------------------
# STARTUP SCREENS
# ------------------------------------------------------------------------------

# Example of automatically running some programs in windows on screen startup.
#
#   The following will open top in the first window, an ssh session to monkey
#   in the next window, and then open mutt and tail in windows 8 and 9
#   respectively.
#
# screen top
# screen -t monkey ssh monkey
# screen -t mail 8 mutt
# screen -t daemon 9 tail -f /var/log/daemon.log
screen -t main_bash 1 bash
screen -t extra_bash 2 bash
screen -t top 0 top
screen -t syslog 9 tail -f /var/log/syslog | grep -E -i 'error|warn'

# Arrange windows in regions
# split vertically
split -v
# move to next region
focus
select extra_bash
# move to next region
focus
select main_bash
