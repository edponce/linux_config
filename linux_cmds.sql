
-- Only root can create databases
-- CREATE DATABASE linux_cmds;

-- Grant privileges to user for a database
-- GRANT ALL PRIVILEGES ON linux_cmds.* TO 'username'@'hostname';

-- Create table for Linux commands
-- NOTE: add column --> GUI (BOOL)
--create table commands (id VARCHAR(24), command VARCHAR(80), description VARCHAR(255), category VARCHAR(32));

--
--INSERT INTO commands synclient -l 
