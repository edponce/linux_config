#!/bin/bash 

usage()
{
    echo
    echo "Usage: $(basename $0) [--help] \"APPLICATION\""
    echo
    echo "Wrapper to invoke an application and clear terminal after its completion."
    echo
}

# Default options
app=

# Get command line options
while [ "$1" ]; do
    case "$1" in
        -h | --help)
            usage
            exit 0
            ;;
        *)
            app="$@"
            shift $# 
            ;;
    esac
done

# Validate
if [ -z "$app" ]; then
    echo "Error: no command was specified."
    exit 1
fi

# app [args] 
appname="$(echo "$app" | sed 's/\b\w\+=\S*\s\b//' | awk '{ print $1 }')"
which "$appname" > /dev/null 2>&1
if [ $? -ne 0 ]; then
    echo "Error: command '$appname' was not found."
    exit 1
fi

# Run 
$app
tput clear

exit 0

